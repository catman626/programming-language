# virtual function

### 虚函数不进行动态绑定的case：
1. 基类的构造函数内调用虚函数，只可能解析为基类的虚函数实现：  
因为子类的虚函数访问子类的数据成员，此时它们还没被初始化，基类constructor使用它们会出问。

### 组织用户进行复制construct/赋值
1. 将复制构造函数和赋值函数私有化
2. 可以不用提供复制cons实现
3. 若赋值函数非虚函数，可不提供实现